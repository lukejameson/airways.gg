# todo.yaml — single source of truth for LLM
# Updated: 2026-06-02T00:00:00Z

goal: >
  Add DST transition regression tests and fix three residual time-handling
  issues to prevent clock-change regressions (autumn fall-back).

why_how: >
  The research brief confirmed the autumn clock change cannot cause the same
  bug — the architecture is now unified around UTC storage with DST-aware
  conversions via luxon. However, three low-severity residual issues were
  identified (UTC date strings used where Guernsey-local dates are expected)
  and a defense-in-depth regression test is recommended. All fixes are tiny
  (1-3 lines each). The test is the main deliverable.

expected: >
  A vitest test file in packages/database/time.test.ts that exercises
  localToUtc() and formatGuernseyTime() across BST/GMT boundaries.
  Three residual fixes in scraper code. All existing tests continue to pass.

status: complete

tasks:
  - phase: 1
    name: Test
    status: done
    items:
      - done: true
        action: Create packages/database/time.test.ts with DST transition regression tests for localToUtc (4 BST/GMT boundary cases) and formatGuernseyTime (4 boundary cases). Verify tests pass.

  - phase: 2
    name: Residual Fixes
    status: done
    items:
      - done: true
        action: Fix Residual 1 — in parseFlightHtml (guernsey-scraper/src/scraper.ts ~L130), replace date.toISOString().split('T')[0] with guernseyTodayStr(date) for defaultFlightDate
      - done: true
        action: Fix Residual 3 — in runBackfill (guernsey-scraper/src/scraper.ts ~L1011), replace new Date().toISOString().split('T')[0] with guernseyTodayStr() for endDateStr default
      - done: true
        action: Fix Residual 2 — in fr24-scraper/src/scraper.ts ~L447 and ~L539, add comment clarifying that now (new Date()) is UTC since TZ=UTC

  - phase: 3
    name: Verify
    status: done
    items:
      - done: true
        action: Run full test suite — verify no regressions from the residual fixes
      - done: true
        action: Build check — verify the project builds cleanly

notes:
  - The autumn clock change (BST→GMT) cannot cause the same bug — architecture is unified
  - All Docker containers run TZ=UTC; all DB connections SET TIME ZONE 'UTC'
  - pg.types.setTypeParser(1114) forces UTC interpretation
  - The ambiguous hour (01:00-02:00) has no flights and scrapers sleep during it
  - Residual fixes are 1-line changes; test file is the main new artifact

discovered: []
