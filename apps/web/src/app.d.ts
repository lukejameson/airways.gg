declare global {
  namespace App {
    // Locals is intentionally empty until authentication is implemented
    // eslint-disable-next-line @typescript-eslint/no-empty-object-type
    interface Locals {}
  }
}

export {};